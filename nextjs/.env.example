# Static site — no backend required.
#
# Optional: only set this when serving from a GitHub Pages *project* site
# (https://<user>.github.io/<repo>). The deploy workflow sets it automatically,
# so you normally don't need it locally. Leave empty for a domain root or a
# <user>.github.io user site.
# NEXT_PUBLIC_BASE_PATH=/your-repo-name
